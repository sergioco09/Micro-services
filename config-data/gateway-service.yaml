server:
 port: 8080
eureka:
 client:
  fetch-registry: true
  register-with-eureka: true
  service-url:
   default-zone: http://localhost:8761/eureka
 instance:
  hostname: localhost

spring:
 cloud:
  gateway:
   discovery:
    locator:
     enabled: true
   routes:
    - id: gestionUsuarios
      url: lb://usuarios-service
      predicates:
       - Path=/gestion/usuarios/v1/**
    - id: gestionAmigos
      url: lb://amigos-service
      predicates:
       - Path=/gestion/amigos/v1/**
    - id: gestionMensajes
      url: lb://gestionMensajes
      predicates:
       - Path=/gestion/mensajes/v1/**
    - id: gestionPublicaciones
      url: lb://gestionPublicaciones
      predicates:
       - Path=/gestion/publicaciones/v1/**
    - id: gestionNotificaciones
      url: lb://gestionNotificaciones
      predicates:
       - Path=/gestion/notificaciones/v1/**              
