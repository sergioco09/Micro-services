server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/sergioco09/Micro-services
          search-paths: config-data
  application:
    name: config-service